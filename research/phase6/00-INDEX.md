# Phase 6 research — index

Twelve parallel research agents, 2026-09-02, on the UltimateWrap model-catalogue and menu
design; reports 14-16 were added later, 16 on 2026-09-04. These files are the **reconstructed agent reports**, written from the returned
results — the raw `.output` transcripts in `%TEMP%` were **0 bytes for every agent but one**,
so this directory is the record.

The working distillation, with running conclusions, is
`~/.uw/keysync/phase6-design-notes.md`.

| # | File | What it settles |
|---|---|---|
| 01 | `01-provider-api-shapes.md` | All 47 providers: baseUrl, protocol, auth, listing endpoint, testModel, flags. **7 call-shape clusters; 41 of 47 through one code path.** Data-quality issues in the vault |
| 02 | `02-free-detection.md` | Per-provider free/paid determinability. The **New-API `/api/pricing`** discovery and its `quota_type` landmine. Precedence order and the blank-not-false rules |
| 03 | `03-ccr-capabilities.md` | CCR internals: discovery loop, `autoFetchModels` merge, `resolve()`'s five stages, `/v1/models` shapes, the bundled catalogue, **all 64 RPC methods** |
| 04 | `04-claude-code-picker.md` | The `/model` picker read from the binary: disabled search, 10-row cap, 60% label truncation, the +2 explained, `behavesAs`, gateway discovery, every extension point |
| 05 | `05-handoff-and-information-architecture.md` | **The decisive report.** Router-is-restart-free confirmed; `CUSTOM_ROUTER_PATH` as the better handoff; and the evidence that a **flat list beats a two-level menu** |
| 06 | `06-maestro-implementation.md` | Maestro's working two-level menu, `/model refresh`, `infer_tier`, the shared vault — and what not to copy |
| 07 | `07-prior-art-survey.md` | models.dev vs LiteLLM head-to-head; OpenRouter/aider/llm/opencode/crush/cline/Roo patterns; Miller columns; fuzzy-filter library comparison |
| 08 | `08-security-review.md` | 13 findings. **CRITICAL: remote model lists are a routing-hijack primitive.** Plus logging, settings integrity, SSRF, terminal-escape injection |
| 09 | `09-build-avoidance.md` | What can be reused instead of written. **The rescued Rust crate has a non-terminating loop.** npm survey with live figures. ~550 new lines recommended |
| 10 | `10-upgrade-resilience.md` | **CC auto-updates.** The already-patched CCR binary. The coupling register (P0/P1/P2), the capability-probe layer, the `models.json` migration |
| 11 | `11-refresh-architecture.md` | Three-tier refresh (free metadata / keyed lists / paid verification), partial-failure merge policy, snapshot cache design, locking |
| 12 | `12-uw-pipeline-audit.md` | Inventory of the current keysync pipeline, the dead `inferTier` proven empirically, and 14 gaps |
| 13 | `13-tui-toolchain.md` | fzf on Windows: the `$SHELL` fork, `--with-shell`, dual renderers, `--listen`; Ink's Windows repaint landmine; what is dead |
| 14 | `14-quota-billing-and-display.md` | 47 providers x 20 candidate paths with a control probe and an invalid-key pass: which quota/billing endpoints exist and which credential each accepts |
| 15 | `15-zero-token-switching.md` | Zero-token, user-driven switching inside CC. Why MCP elicitation, slash commands and the conversational design are all disqualified |
| 16 | `16-harness-context-management.md` | **Ten harnesses read at source** on context windows and compaction. The unknown-model split (guess vs refuse), absolute buffers over percentages, the confirmed negative that **nobody parses the limit out of the error text**, and Roo's per-endpoint fix for the aggregator problem. Measures UW's own catalogue: 98.5% text-model coverage against live models.dev, a live 4x over-declaration in the picker, and **probe P6 answered**. Addendum (2026-09-05): dynamic `[1m]` tagging shipped, `Xh` substring canonicalization found, capabilities split six-free/three-gated |
| 17 | `17-compaction-live-tracking-attempts.md` | The resolver is proven NOT read-once (corrects 16's premise) — but the only writer is a single global env var, and nothing can rewrite it live without process injection. Bun preload injection closed empirically (compile flag confirmed off, live positive-controlled test). Debugger attach open only at launch, a standing risk. Hook system exhaustively closed for command-chaining across three independent audits, including a deliberate anti-pattern defense in Claude Code's own plugin path. **Two paths remain**: the manual two-keystroke `/autocompact` flow (recommended) and an OS-level console-input-delivery helper (fully designed, not built — crosses a line this project has not crossed before) |
| 18 | `18-behavesas-capability-profile.md` | The 83-row blanket `behavesAs` over-declaration affects effort/thinking/prompt-bundles, not just context. Dropping `behavesAs` tested and disproven as a safe fallback — it's the *maximal* assumption set on UW's setup. `modelOverrides` is a second path into the same machinery, not identity-only. Exhaustive settings.json sweep: no hidden capability field. **CCR already neutralizes the wire-level risk for 44/45 providers** — no fix needed there. Recommended: corrected bucket targets (`sonnet-4-5`/`haiku-4-5`/`opus-4-1` vs `opus-4-6`/`sonnet-4-6`, never `opus-5`/`fable-5*`) plus model-type grey-out for non-chat rows. **§6 revised 2026-09-06** after two scoping investigations: the exact per-model mechanism is *not* out of architectural reach as first written — Foundry is a config-scale shape (same SDK base class, no request middleware, documented auth-skip flag, ids already identical, zero relay protocol work). It stays unadopted for three different reasons: the declaration goes stale on the first in-session switch, UW has no accurate capability data to declare, and any non-`firstParty` shape kills `cl()`/`gt()` and with them the subscription/quota surface. `_CLAUDE_CODE_ASSUME_FIRST_PARTY_BASE_URL` is dead — ~50 gates, kills gateway model discovery permanently. Also: the eight gated predicates named exactly, and **context is not among them**, so this work and report 17's are orthogonal. **§9 added 2026-09-06** — the bucketing input signal measured: a real `capabilities.reasoning` boolean is already loaded from CCR's merged catalogue and silently discarded, 55.4% strict / 81.9% loose coverage of the 83 rows against context's 90.4%; the two compose. Prerequisite bug found — `!!` coercion at `menu/catalog.mjs:178` writes unknown as `false`, so an unfixed bucketer buckets every unknown as small. A direct models.dev fetch measures *worse* (misses 25% of UW's rows). Four non-text rows enumerated. Coverage gaps track source provenance, not provider obscurity. Eleven providers already serve live capability fields UW never fetches. **§10 added 2026-09-06** — the per-model predicate table read from the baked catalog: §8's bucket targets confirmed on direct evidence (`sonnet-4-5`/`haiku-4-5`/`opus-4-1` carry only `context_management`; `sonnet-4-6`/`opus-4-6` add effort + adaptive thinking; `opus-5`/`fable-5*` carry the prompt bundles and are correctly excluded). Two corrections: **none of the eight predicates is UI-only** — all are wire-affecting, so §7's safety comes from CCR neutralizing them in transit, not from them never being sent; and **a false `thinking` predicate does NOT suppress rendering or parsing of returned reasoning** (`RX()` targets only `tool_result`/`server_tool_use`, protects thinking, and is inert off `api.anthropic.com`), retiring the sharpest risk in the bucketing design. Bucketing only ever under-declares, the safe direction for all eight |
| 19 | `19-deepseek-harness-counterfactual.md` | **DeepSeek Harness cloned and read at source** to test whether reports 17/18's limitations are inherent to wrapping any harness. They are not. dsh resolves the context window per `(provider, model)`, `await`ed fresh at every `agent/pre-step`, with per-model threshold overrides — Limitation A cannot arise. It has a per-model declarable capability + 22-field wire-compat profile and **no borrowing mechanism at all** — Limitation B cannot arise, and DeepSeek's own source comment states the same diagnosis this project reached independently. Both limits are Claude Code-specific and structural: they fall out of it having a *privileged baked Anthropic catalog* as its only vocabulary for describing a model. **But dsh would have been a worse base** — no terminal agent (browser UI; report 17 §4's entire solution space has no host), no subscription concept, no `apiKeyHelper`. Also: the hook-cannot-submit-commands constraint is **convergent across both harnesses**, closing a question report 17 left open. Three design lessons transfer: the unknown/negative tri-state (report 18 §9.2's `!!` fix), making a fallback guess a configurable field rather than a buried constant, and per-target policy keyed on exact `{provider, model}` validated at load |
| 20 | `20-reachability-and-liveness-labels.md` | **Live probe: 270 requests, 44 providers.** Of 1,588 catalogued models only **~205 are reachable** at current balances (~260 if the unfunded accounts were paid) against 91 routing entries — and **26% of failures are money, not death** (alibaba: 343 catalogued, two real-client-verified on 2026-09-01, all 403 five days later over a bill). Produced the standing rule that a responding provider is never pruned, and a ten-class failure table in which **nothing prunes**. Applies the user's staleness budget to decide which labels can be kept honest: provider-level liveness is 44 requests and survives; model-level absence is ~1,588 and must not be labelled at all — let the user meet the error. **The cap was never the binding constraint — the ranking is**: `inferTier` yields a usable value for **0 of 4,298** models (reads `pricing.inputPerMillion`, schema stores `pricing.offers[].per1MTokens`), so selection is shortest-id-first and UW routes ~20 of the ~205 reachable. Names phase B's first structural task: the `refresh/cli.mjs` that B1-B5 all trace back to was never built |

Also preserved: `~/.uw/prior-art/uw-rust-tui/` — the complete Rust crate rescued from a
`%TEMP%` scratchpad that would have been swept, including the **working** `model_tier.rs`
(the tier inference the JS version gets wrong).

## The three findings that most change the design

1. **A two-level menu cannot be built inside Claude Code** (04), but it does not need to be:
   Router rules repoint a stable alias with **no gateway restart and no relaunch**, and CCR
   **echoes the requested model back**, so the alias survives end-to-end and CC never learns
   the resolution (05).
2. **The usage data argues against the requested shape.** 1,019 requests, 26 distinct models,
   **top 5 = 91.3%**. A two-level menu taxes the 91% case to serve a rare one, and the
   hierarchy is already encoded in the `provider/model` string (05).
3. **Fixing `inferTier` arms a routing hijack** (08, 12). The denylist must land first. This is
   the clearest case in the whole pass of a correctness fix being a security regression if
   sequenced wrongly.
